import 'package:flutter/material.dart';

class tryoutstuff extends StatelessWidget {
  const tryoutstuff({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
